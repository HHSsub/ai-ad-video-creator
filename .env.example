# ─────────────────────────────────────────────
# Server-side secrets (never commit real values)
# ─────────────────────────────────────────────
# Gemini API Key (서버에서만 사용; 클라이언트에 노출 금지)
GEMINI_API_KEY=

# 모델 선택
# - 현재 코드 동작: 기본 모델을 최대 8회(지수 백오프) 재시도 후 실패하면 폴백 1개로 전환
# - pro 과부하/한도 이슈를 피하려면 아래 조합 권장:
#   GEMINI_MODEL=gemini-2.5-flash
#   FALLBACK_GEMINI_MODEL=gemini-2.5-flash-lite
# - pro 먼저 쓰고 막히면 flash로 넘기려면:
#   GEMINI_MODEL=gemini-2.5-pro
#   FALLBACK_GEMINI_MODEL=gemini-2.5-flash
GEMINI_MODEL=
FALLBACK_GEMINI_MODEL=

# ─────────────────────────────────────────────
# Admin Field Configuration (Step1 폼 필드 설정)
# ─────────────────────────────────────────────
# 필드 활성화/비활성화 (true/false)
FIELD_BRAND_NAME_ENABLED=true
FIELD_INDUSTRY_CATEGORY_ENABLED=true
FIELD_PRODUCT_SERVICE_CATEGORY_ENABLED=true
FIELD_PRODUCT_SERVICE_NAME_ENABLED=true
FIELD_VIDEO_PURPOSE_ENABLED=true
FIELD_VIDEO_LENGTH_ENABLED=true
FIELD_VIDEO_ASPECT_RATIO_ENABLED=true
FIELD_CORE_TARGET_ENABLED=true
FIELD_CORE_DIFFERENTIATION_ENABLED=true
FIELD_VIDEO_REQUIREMENTS_ENABLED=true
FIELD_BRAND_LOGO_ENABLED=true
FIELD_PRODUCT_IMAGE_ENABLED=true

# 필드 라벨 커스터마이징
FIELD_BRAND_NAME_LABEL=브랜드명
FIELD_INDUSTRY_CATEGORY_LABEL=산업 카테고리
FIELD_PRODUCT_SERVICE_CATEGORY_LABEL=제품/서비스 카테고리
FIELD_PRODUCT_SERVICE_NAME_LABEL=제품명/서비스명
FIELD_VIDEO_PURPOSE_LABEL=영상 목적
FIELD_VIDEO_LENGTH_LABEL=영상 길이
FIELD_VIDEO_ASPECT_RATIO_LABEL=영상 비율
FIELD_CORE_TARGET_LABEL=핵심 타겟
FIELD_CORE_DIFFERENTIATION_LABEL=핵심 차별점
FIELD_VIDEO_REQUIREMENTS_LABEL=영상 요구 사항
FIELD_BRAND_LOGO_LABEL=브랜드 로고 업로드
FIELD_PRODUCT_IMAGE_LABEL=제품 이미지 업로드

# 기본값 설정 (필드가 비활성화될 때 사용)
DEFAULT_BRAND_NAME=브랜드
DEFAULT_VIDEO_LENGTH=10초
DEFAULT_VIDEO_ASPECT_RATIO=가로 (16:9)

# ─────────────────────────────────────────────
# API Configuration
# ─────────────────────────────────────────────
# API 도메인 (백엔드에서 외부 API 호출 시 사용)
# 예: https://upnexx.ai, https://api.example.com
# 기본값: https://upnexx.ai
API_DOMAIN=https://upnexx.ai

# ─────────────────────────────────────────────
# Front-end
# ─────────────────────────────────────────────
# Vite 개발 서버 프록시 사용 시 비워둡니다(권장).
# 별도 API 호스트로 직접 붙일 때만 http://<api-host>:3000 처럼 지정하세요.
# 상대 경로(예: /nexxii/api)를 입력하면 API_DOMAIN과 결합됩니다.
VITE_API_BASE_URL=

# (선택) 클라이언트에서 썼던 흔적. 서버는 GEMINI_API_KEY만 사용합니다.
VITE_GEMINI_API_KEY=
